<div class="container">
  <div class="row">
    <ul class="nav nav-pills nav-center">
 
      <li role="presentation" class="active text-center"><%= link_to "All Category", root_path %></li>
      <% @categories.each do |category| %>
        <li role="presentation" class="text-center"><%= link_to category.name, category_path(category) %></li>

      <% end %>
    </ul>
    <%= render partial: "shared/restaurant_list" %>
      
   
      
  </div>
</div>