<% if restaurant.is_favorited?(current_user)%>
  <%= link_to "Unfavorite", unfavorite_restaurant_path(restaurant), class: "btn btn-info", method: :post%>
<% else %>
  <%= link_to "Favorite", favorite_restaurant_path(restaurant), class: "btn btn-primary",method: :post %>
<% end %>