<% if restaurant.is_liked?(current_user) %>
  <%= link_to "Unlike", unlike_restaurant_path(restaurant), class: "btn btn-info", method: :post%>
<% else %>
  <%= link_to "Like", like_restaurant_path(restaurant), class: "btn btn-primary",method: :post%>
<% end %>